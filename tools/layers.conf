# Base layer indices, as shown in Oryx (0-based).
# Single source of truth: firmware defines, host scripts and tests all read this.
# Reorder layers in Oryx -> change this file, nothing else.
mac=0
omarchy=2
